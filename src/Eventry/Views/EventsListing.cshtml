@using Eventry.Web.PublishedModels

@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<EventryEventListing>

@foreach (IEventryEventBaseComposition eventItem in Model.Children)
{
    await Html.RenderPartialAsync("_EventListingCard");
}
